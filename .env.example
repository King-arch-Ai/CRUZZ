# Copy to .env and fill in when deploying
PORT=8080
NODE_ENV=production
# On Render, prefer a persistent store (Redis) for auth state; this demo uses filesystem
SESSION_DIR=./storage/sessions
# Optional: set BASE_URL if serving behind a custom domain
BASE_URL=
